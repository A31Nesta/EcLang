#pragma once

#include <string>
#include <map>
#include <vector>

// EXAMPLE CONFIGURATION FOR MEATGRINDER
namespace compilerSettings {
    extern std::vector<std::string> nodeTypes;
    extern std::map<std::string, std::string> properties;
}